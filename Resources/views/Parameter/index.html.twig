{% extends 'TNGroupBundle::base.html.twig' %}

{% block body -%}
    <h1>Parameter list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Parametername</th>
                <th>Type</th>
                <th>Value</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('parameter_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.parameterName }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.value }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('parameter_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('parameter_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('parameter_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
